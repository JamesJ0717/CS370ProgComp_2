version: 2
jobs:
  build1:
    docker:
      - image: nodejs:lts
    steps:
      - checkout
      - run:
          name: Building Dist
          command: npm build
      - save_cache:
          key: OCPS-{{ .Environment.CIRCLE_SHA1 }} 
          paths:
            - ~/opcs-workflows
